import React, { Component } from 'react';
import PropTypes from 'prop-types';

export class exampleGraph extends Component {
	static propTypes = {};
    constructor(props) {
        super(props)
        this.state = {
            svg:
        }
    }
	render() {
		return <div />;
	}
}

export default exampleGraph;
